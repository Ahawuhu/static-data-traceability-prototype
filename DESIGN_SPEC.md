# 智慧城后台页面设计规范

## 1. 设计原则

- 定位：物业资产运营后台，界面克制、清晰、适合高频查询与表格操作。
- 信息层级：全局导航 > 模块导航 > 页面标题与操作 > 筛选区 > 数据区。
- 布局：固定顶栏与左侧导航，内容区自适应；桌面端优先，兼容 1280px 及以上屏幕。

## 2. 色彩

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 品牌主色 | `#3f8df6` | 当前菜单、主按钮、链接 |
| 导航背景 | `#102f52` | 左侧一级导航 |
| 导航深色 | `#0b2542` | 展开分组背景 |
| 页面背景 | `#f3f5f8` | 工作区底色 |
| 表头背景 | `#eef3f9` | 数据表表头 |
| 主文字 | `#263445` | 标题、主要数据 |
| 次文字 | `#687586` | 描述、辅助信息 |
| 边框 | `#dce3eb` | 表格、输入框、分割线 |
| 成功 | `#22a06b` | 已完成、正常 |
| 警告 | `#e58a24` | 待补充、部分缺失 |
| 危险 | `#e25454` | 错误、删除 |

## 3. 字体与字号

- 字体：`Arial, "Microsoft YaHei", sans-serif`。
- 页面标题 18px/600；区块标题 16px/600；正文与表格 14px/400；辅助信息 12px/400。
- 行高：正文 1.5；表格单行最小高度 44px。
- 字间距固定为 0。

## 4. 布局尺寸

- 顶栏：70px；左侧导航：240px；二级页签栏：48px。
- 内容区外边距：18px；内容面板内边距：20px。
- 筛选控件高度：36px；按钮高度：36px；图标按钮：36px × 36px。
- 页面主体使用完整白色工作面板，不使用装饰性悬浮卡片。

## 5. 组件规范

- 按钮：主按钮蓝底白字，次按钮白底灰边；圆角 3px；命令按钮可使用“图标 + 文字”。
- 输入框：1px 灰色边框，圆角 3px；聚焦时品牌色描边。
- 表格：全边框、浅蓝灰表头；文字左对齐，数量和状态可居中；操作使用品牌色文字按钮。
- 状态：使用浅色底标签，圆角 2px，避免高饱和大色块。
- 弹窗：居中覆盖层，标题/内容/操作区明确分隔；危险关闭使用标准关闭图标。
- 上传：支持点击选择与拖拽，清晰呈现文件名、类型、大小和上传状态。

## 6. 交互与状态

- 查询、分类切换、分页均即时反馈；空结果提供明确空状态与重置入口。
- 查看进入小区详情，保留返回路径；分类标签显示记录量与完整度。
- 编辑需展示当前字段值，保存后出现轻量提示并更新“最后更新时间”。
- 上传必须选择小区和数据分类；仅接受 `.docx`、`.xlsx`、`.xls`、`.csv`。
- 所有可点击控件具备 hover、focus、disabled 状态；弹窗支持 `Esc` 关闭。

## 7. 响应式与可访问性

- 小于 1180px 时侧栏收窄为 76px，仅显示图标；表格区域横向滚动。
- 小于 768px 时筛选项纵向排列，详情摘要改为两列，弹窗宽度使用视口宽度。
- 颜色对比满足常规文本可读性；按钮包含可见文本或 `aria-label`；焦点顺序与视觉顺序一致。

